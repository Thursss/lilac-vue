import { login, register, getUserInfo, logout } from '@/api/user/user.js'
import { getDocList } from '@/api/doc/doc.js'
import { getSubList } from '@/api/doc/sub.js'

export { login, register, logout, getUserInfo, getDocList, getSubList }